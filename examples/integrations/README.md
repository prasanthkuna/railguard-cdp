# Integration examples (plan §16)

| Folder | Partner |
|--------|---------|
| `../x402-agent` | x402 machine payments |
| `../vendor-payment-agent` | CDP invoice path |
| `../procurement-agent` | AP2 mandates |
| `agentkit` | Coinbase AgentKit → MCP |
| `privy-wallet` | Privy adapter env |
| `viem-guarded` | viem + settlement verify |
| `arbitrum-evidence` | Arbitrum Sepolia manifest |
| `risk-panel` | Blockaid / CRE env |

Run from `coinbase/`:

```powershell
bun run railguard doctor
bun run railguard protect
```
